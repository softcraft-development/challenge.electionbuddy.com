# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  election: one
  user: one
  audit_changes:
    settings:
      visibility: private
  operation: update
  target: one (Election)

two:
  election: two
  user: two
  audit_changes:
    settings:
      visibility: public
  operation: update
  target: one (Question)
